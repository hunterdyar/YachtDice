using UnityEngine;
using UnityEngine.UIElements;

public class UIGameplayUIBinder : MonoBehaviour
{
    private UIDocument gamplayDoc;

    [SerializeField] private string rollTrayName;
    [SerializeField] private string keepTrayName;
    
    
    // Start is called before the first frame update
    void Start()
    {
        //gameObject.AddComponent<DiceC>()
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
